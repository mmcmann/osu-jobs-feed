'use strict';

describe('JobFeedApplication.feedList module', function() {

  beforeEach(module('JobFeedApplication.view1'));

  describe('feedList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});